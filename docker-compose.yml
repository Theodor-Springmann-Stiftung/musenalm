services:
  musenalm:
    build: .
    expose:
      - "8090"
    volumes:
      - musenalm:/app/data
      - ./musenalm-data:/app/musenalm-data:ro
    networks:
      - caddynet

volumes:
  musenalm:
    external: true

networks:
  caddynet:
    external: true
