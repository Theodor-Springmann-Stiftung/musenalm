services:
  musenalmstage:
    build: .
    expose:
      - "8090"
    volumes:
      - musenalmstage:/app/data
      - ./musenalm-data:/app/musenalm-data:ro
    networks:
      - caddynet

volumes:
  musenalmstage:
    external: true

networks:
  caddynet:
    external: true
